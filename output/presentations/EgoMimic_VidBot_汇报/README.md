# EgoMimic × VidBot 汇报

## 编译

在当前目录执行：

```bash
xelatex slides.tex
xelatex slides.tex
```

使用 XeLaTeX 是为了正确排版中文。`slides.tex`、`CUHKBeamer.sty` 和 `assets/` 共同构成可编辑的完整工程。

## 内容说明

- EgoMimic 的论文结果与本地 medium 复现结果分开呈现。
- 本地验证视频只作为离线动作预测可视化，不代表仿真或真实机器人成功率。
- VidBot 尚未完整运行，因此只汇报论文结论、代码边界和建议复现顺序。
- 首页的汇报人姓名可在 `slides.tex` 的 `\author` 行填写。

