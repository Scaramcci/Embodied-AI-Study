# 从一段视频开始：第一条可执行路线

这份文档只回答“接下来怎样开始”。你原有的 LeRobot 学习和论文阅读继续进行；这里另外开一个很小的练习：**亲手看见一段视频怎样变成可检查的数据，再看这些数据与机器人训练样本之间差了什么。** 按完成条件前进，不需要先确定研究方向。

## 先准备一个工作样本

拍一段 **10～20 秒**的视频：固定手机、固定桌面、一个人用手把一个物体从 A 移到 B。画面里尽量只有一个主要物体，完整拍到手靠近、接触、移动、放开。把原视频原样保存，例如放在 `practice_video/clip.mp4`；不要覆盖它。若不方便拍，也可以用一段来源明确、允许研究使用的公开视频。先用这一段，遇到真实问题再增加样本。

开始前在纸上或笔记里写一句话：**“这段视频里，物体从哪里移动到哪里？”** 后面每一步都要能回到这段原视频核对。

## 第一步：把视频读成帧，并认出数组

1. 建立 `practice_video/frames/`。先用 FFmpeg 抽取约每秒 2 帧：`ffmpeg -i practice_video/clip.mp4 -vf fps=2 practice_video/frames/%04d.png`。如果电脑还没有 FFmpeg，先安装它；这一关不需要装任何 CV 模型。
2. 打开第 1 张、中间 1 张、最后 1 张图。确认顺序与原视频一致，看看接触瞬间是否刚好被抽帧跳过。跳过了就改成每秒 5 帧再试一次。
3. 用你现有的 Python 环境读取其中一张图，打印 `shape`、`dtype`、最小值和最大值。第一次看到 `(高, 宽, 3)` 与 `uint8` 就够了；再裁出物体附近的小图，保存并确认裁剪坐标没有写反。
4. 记下原视频帧率、抽帧规则，并建立一个小表：`保存的图片名 → 原视频中的时间（秒）`。FFmpeg 的 `fps` 过滤会按时间重采样；此处先记录对应时间，不要假设输出图片编号等于原始帧号。

**完成条件：** 能指出任意一张抽出的图在原视频的哪个时刻，能解释图像数组的三个维度。保留原视频、抽帧图和时间对应表。

## 第二步：先自己标，再让一个 CV 工具处理

1. 从这些图中挑 **5 张**：接触前、刚接触、移动中、放开前、放开后。先自己给物体画粗框，写下你认为发生接触的时间。标得粗没关系，这是你的参照。
2. 只选**物体分割**作为第一个模型任务。用一个现成实现的官方示例在这 5 张图中的一张上得到物体 mask；先跑通单图，再尝试把 mask 扩展到整段视频。现在不用同时安装手部重建、深度、去人和点跟踪工具。
3. 每张图保存三样东西：原图、黑白 mask、mask 叠加到原图的可视化。逐帧看物体有没有被漏掉、手是否被错分成物体、遮挡后是否跟错目标。选一帧错误，写一句“错在何处”。
4. 记录工具实际需要的输入：图像、文本提示、框或点；以及它实际输出的 mask 大小、数据类型、取值范围。若官方示例尚未跑通，保存报错与环境信息，先定位这一个障碍。

**完成条件：** 能展示同一时刻的原图和分割结果，能说出至少一个可信或不可信的地方。到这一步为止，mask 只是“哪些像素像物体”，尚不是机器人动作。

## 第三步：把输出整理成一条能读回的数据记录

1. 给每张处理过的图写一条记录。先用 CSV 或 JSON 即可；字段只放 `clip_id`、`time_s`、`image_path`、`mask_path`、`object_name`、`contact_note`。还没有可靠数值时，把 `contact_note` 写成人工备注，不伪装成自动计算的接触真值。
2. 随机打开一条记录，按照 `image_path` 和 `mask_path` 重新读回两张图；检查尺寸相同、时间对得上、mask 没有串到另一帧。再故意改错一个路径或时间，看自己能否发现。
3. 在记录旁写下：这条数据能教模型什么？例如物体位置或外观变化。它现在**没有**机器人关节状态、末端位姿、夹爪开合、可执行动作，也没有可靠的三维尺度。把缺失项写出来，就是你第一次看见“互联网视频→机器人动作”的鸿沟。

**完成条件：** 给别人一条记录，他能找到原视频时刻、打开图片和 mask，并知道哪些字段来自人工、哪些来自模型。

## 第四步：拿一条现成训练样本对照，不急着拼接

1. 在你正在学的 LeRobot 数据中找**一条**机器人样本，打印图像、状态、动作各自的 `shape`，以及它们对应的时刻；把动作的单位、坐标系、绝对值还是增量写在旁边。若这些信息暂时找不到，就明确标“待查”，不要根据数组形状猜。
2. 再按 [EgoMimic 项目结构与运行逻辑](papers/Egomimic/EgoMimic-main/Learning_doc/01_项目结构与运行逻辑.md) 的“数据结构 → 训练样本 → 模型输出”顺序，看它怎样从已有的机器人／人类示范中取出 observation 和 action。只追一条样本，不通读整个仓库。
3. 在一页纸上并排写两行：`我的视频样本：图像 + mask + 时间 + 人工备注`；`训练样本：图像 + 状态 + 对齐时间的动作标签`。用箭头标出真正缺的桥梁，并选一个你最想追问的问题，例如“怎样从视频得到可执行动作？”或“没有动作标签时视频还能提供什么监督？”

**完成条件：** 能说明一条训练样本的输入和目标是什么；能解释为什么把自己的 CSV 直接塞进机器人动作字段不成立。到这里，这个启动练习已经完成，不要求训练新模型。

## 做完后，三个已有文档怎样接

- 如果最困惑的是**数据怎样进入网络、loss 和 checkpoint 怎样产生**：接着看 [EgoMimic 项目结构与运行逻辑](papers/Egomimic/EgoMimic-main/Learning_doc/01_项目结构与运行逻辑.md)，拿你在第四步看过的那条样本，追它进入一个 batch 和一次模型预测的过程。
- 如果最困惑的是**人类数据和机器人数据字段不一样，为什么还能一起训练；训练是否需要仿真试错**：接着看 [训练试错与人机联合对齐的问题解答](papers/Egomimic/EgoMimic-main/Learning_doc/02_问题解答_训练试错与人机联合对齐.md)，带着第四步列出的缺失字段去读。
- 如果已经能指出一个具体缺口，想知道**下一阶段选什么小实验、怎样连到 VLA 或研究问题**：再看 [具身学习阶段分析与 Codex 协作规划](03_具身学习阶段分析与Codex协作规划.md)。先只看其中与你当前缺口对应的阶段；不要把整份规划当待办清单。

之后再回到 [论文方法地图](人类视频到机器人操作_论文方法Pipeline对照.md) 选一条路线。比如想研究物体怎么动，就读物体／轨迹路线；想研究手怎么变成机器人动作，就读手部重定向路线。此时选论文，是为了回答你在第四步写出的那个问题。

**现在就开始：** 找到或拍下那段 10～20 秒视频，保存原文件，抽出第一批帧，并找出“刚接触物体”的一张图。今天能准确指出那一帧和它的时间，就已经启动了。
