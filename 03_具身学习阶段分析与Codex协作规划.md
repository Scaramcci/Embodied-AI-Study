# 当前阶段分析、整体优先级与 Codex 协作规划

**你已经开始形成方向地图，但还没有建立“一个视频样本如何变成监督、这个监督如何影响机器人表现”的可检查闭环。下一步最值得做的，是把一条短链路跑明白，并学会判断它哪里错了。** 不需要先补完整套 CV，也不需要等读完所有相关论文。

本规划不设学习时间、不安排实机；你现有的 LeRobot 项目继续推进。论文与技术细节分别见 [阅读与复现建议](01_论文阅读顺序与复现建议.md)、[Pipeline 技术学习](02_Pipeline技术学习顺序与掌握深度.md)。

## 1. 你现在处于什么状态

按你这次描述，可以把能力分成四层：

| 层次 | 你目前的位置 | 下一步需要的证据 |
|---|---|---|
| 方向认识 | 已知道多条路线及组件的大致作用 | 能不看笔记解释两条路线的根本差别 |
| 工具使用 | 尚未系统建立输入、输出和失败判断 | 在自己的样例上调用一个组件并检查输出 |
| 训练与评估 | 正通过 LeRobot 接触 | 能解释一个训练 batch、动作含义和一次失败 rollout |
| 研究判断 | 尚缺可比较的实验经验 | 能提出一个变量明确、可被否定的问题 |

这是跨方向入门常见的过渡阶段。当前瓶颈主要是术语缺少真实对象：看到“深度”“动作”“对齐”时，没有对应的数组、可视化、单位和错误实例。再读十篇结构相近的摘要，未必能解决这个瓶颈。

你的“先学怎么用，再研究推导”合理。不过“会用”不能只指命令成功退出；至少要会检查输入条件、输出语义和错误传播。将来要修改方法时，仍需要针对那个模块补数学和实现原理，不宜永久把它们排除。

已有学习记忆中记录过你偏好“从论文出发、遇到障碍再补基础”，这里延续这个方法；历史实验完成情况没有作为本规划的能力依据。本轮检查没有找到旧记忆提及的 `planning/` 目录，因此不假定其中的旧练习现在仍可用。

## 2. 对导师命题的理解：视频处理重要，但必须有学习出口

“利用互联网视频，通过数据处理训练机器人”至少包含四个问题：

1. 视频能提供什么：任务语义、物体变化、人手轨迹、接触线索或潜动作？
2. 提取的东西有多可靠：相机运动、尺度、遮挡、时间错位怎样污染它？
3. 如何迁移到机器人：作为动作标签、辅助损失、视觉预训练或规划目标？
4. 如何证明有帮助：相同机器人数据预算下，任务表现或泛化是否改善？

CV 可以成为你的主要研究重心，但必须知道自己交给训练系统的是什么，以及怎样验证价值。你不需要成为所有方向的专家；需要在一个模块上深入，同时理解上下游接口。

另一个需要澄清的点：VLA 是视觉、语言、动作模型。常见使用方式是“图像 + 指令 + 可选机器人状态 → 一段动作”；它不天然拥有可靠的三维理解，也不天然能把互联网视频直接变成电机命令。是否用 VLA，应由研究问题决定。

## 3. 优先级：一条主线、一个实践支撑、有限的扩展阅读

| 优先级 | 现在做什么 | 为什么 |
|---|---|---|
| P0 | 搞清一个样本与一种评价：时间、坐标、动作、成功条件 | 所有后续学习共用，没有这一层容易误判结果 |
| P1 | 细读代表论文，同时做视频组件/官方推理小实验 | 把名词变成可检查的对象，直接服务导师方向 |
| P1 | 继续 LeRobot，并建立一次训练与评估闭环 | 为“处理后数据是否有用”提供参照 |
| P2 | 选择一条人视频→监督路线，做单变量比较 | 开始获得自己的发现，而不只是整理别人的方法 |
| P3 | 根据实验暴露的缺口补论文与替代技术 | 调研变得有问题驱动，不再无限扩张 |
| 暂缓 | 全量 CV 课程、全部 16 篇完整复现、大基础模型从零预训练 | 高成本，且不能替代关键接口与实验判断 |

这些事情不是完全串行的。阅读、调用、评估应该围绕同一个问题交替发生。但同一阶段只维持一个主实验，不同时开启分割、手重建、视频生成、VLA 架构创新四条工程线。

默认主线选择：**先显式、可视化的视频表示，后进入训练。** 原因是点、mask、深度更容易让初学者检查错误。它是学习上的选择，不预先判断显式方法一定优于潜动作方法。

## 4. 按完成条件推进的阶段规划

### 阶段 A：把地图变成几个真实对象

阅读 Track2Act、VidBot、Masquerade、VITRA、LAPA 的关键接口；不必一次完成全部细读。先用一小组带来源信息的视频，覆盖固定相机、移动相机、遮挡和明显失败。

运行一个分割/点跟踪实例，再运行 Track2Act 或 VidBot 的官方推理。自己说明哪一步是提取真实视频运动，哪一步是预测未来，哪一步才与机器人动作相连。

**完成条件：** 能拿出输入、输出与失败可视化，解释一个错误如何传到下一步；能说出“这次实验尚未证明什么”。若官方示例仍跑不通，先定位依赖/资源问题，不用自造近似结果冒充成功。

### 阶段 B：建立小策略或 VLA 的可重复训练与仿真

借用 LeRobot 学数据格式，按下一节做小型 VLA 闭环。把训练 loss、验证误差和任务成功率当作三个不同信号。

**完成条件：** 已有 checkpoint 可重新加载；同一配置能重复评估；能定位一次失败是输入映射、动作反归一化、控制接口还是策略本身的问题。先达到可诊断，不追论文最高分。

### 阶段 C：把导师方向缩成一个可以反驳的问题

默认候选：**时序质量筛选是否比随机挑选同样数量的人类视频更有助于下游学习？** 这是候选问题，尚未证明有创新性，也不承诺会有正结果。

先用你已跑通的一条路线，筛选只使用该路线已有输出，不额外叠加很多网络。例如有轨迹时检查可见性与突变，有手标签时检查左右手连续性。筛选分数可能偏向简单任务，必须一起检查保留的数据组成。

进入下游前，只能得出“提高了某些标签质量指标”；当能固定训练配方并测任务结果后，才能讨论“有助于机器人学习”。

**完成条件：** 有一个基线、一项变化、一个预先规定的评价方式；结果不符合预期时，知道该否定假设还是检查实现。

### 阶段 D：确认问题后再扩展

若收益稳定，补最近的同问题工作，查是否已有相同结论；再增加任务、场景和随机种子。此时才深入相关理论或修改模块。

若无收益，先看问题是否真实存在、筛选是否删掉困难样本、标签改善是否与任务相关。不要一遇负结果就换成更大模型。可靠的负结果也会帮助选路线，但不自动构成论文贡献。

**完成条件：** 能向导师提出一个范围清楚的问题，带初步证据、最接近的已有工作、下一步对照和资源需求。正式研究方向应在这一刻收敛，而不是在看懂几个模型名字时仓促决定。

## 5. VLA 项目怎样安排才不会过重

你的“训练→微调→仿真→实机”愿望可以拆成两个项目。**微调本身也是训练；从头训练 VLM/VLA 底座不是掌握完整流程的必要条件。**

### 项目一：以现有 VLA 建立工程闭环

建议首选 **LeRobot + SmolVLA + LIBERO**，尽量沿用已有工具生态。SmolVLA 官方提供轻量预训练模型及微调接口；LeRobot 提供 LIBERO 仿真评估，当前文档要求 Linux 并给出无显示渲染设置。[SmolVLA 文档](https://huggingface.co/docs/lerobot/smolvla)、[LIBERO 文档](https://huggingface.co/docs/lerobot/libero)

这个项目的执行顺序应是：

1. **先评估一个匹配的现成 checkpoint。** 从官方 LIBERO 模型与对应数据/处理配置开始；检查相机名、图像朝向、状态字段、相对/绝对动作、归一化统计。不要把 SO-101 数据和 LIBERO 动作接口混接。
2. **查看训练数据与一个 batch。** 你要能读出当前观测、指令、未来动作块、padding 和有效维；不用逐层研究网络。
3. **跑一次训练更新、保存和恢复。** 再在很小的数据上检查能否过拟合，目的是排查训练链路，不把过拟合当泛化。
4. **从 `smolvla_base` 做一个限定任务范围的微调。** 使用训练数据匹配的配置和统计，不把原有 LIBERO checkpoint 的表现算作你训练的成果。
5. **用固定协议比较微调前后。** 同一任务、初始状态、评估条件，记录失败视频与成功次数/总次数。若预训练数据含相同任务，不能据此宣称新任务零样本泛化。

官方模型入口：[smolvla_base](https://huggingface.co/lerobot/smolvla_base)、[smolvla_libero](https://huggingface.co/lerobot/smolvla_libero)。模型卡中的示例可能混有不同版本或平台命令，应以锁定的 LeRobot 版本和对应配置为准。本规划不提供未经本机验证的整套安装命令。

**这个项目教你 VLA 怎么用，但本身还没有回答互联网人类视频的价值。** 仿真任务范围可以先很小，结果也只对该范围成立。若你现有 LeRobot 项目已覆盖其中的学习目标，直接复用，不重做一套。

### 项目二：学习视频预训练→动作微调的机制

在项目一之后，选 **LAWM 的缩小实验**；若最终研究对象明确是离散潜动作，再选 LAPA 的量化器与后续适配。不要两个同时从头展开。

LAWM 官方提供从无动作视频预训练、用动作示范微调、在 LIBERO 评估的入口；原论文附录 D 报告默认单张 A100。它仍需核查你 48GB 卡的峰值显存、数据可取性和依赖。[官方实现](https://github.com/baheytharwat/lawm)、[本地原论文](papers/LAWM/LAWM.pdf)

学习目标是亲自看见：不使用真实动作标签的阶段怎样产生监督；加入机器人动作数据后如何适配；预训练是否在固定机器人示范预算下有帮助。小规模结果不代表复现论文全量结果，也不代表从零训练了通用 VLA。

若你要把 LAWM 思路直接接到 SmolVLA 上，那已经是新的模型适配工作，应排在官方配置复现之后，不作为第一个“学习调用”的项目。

实机迁移留待你跑完现有 LeRobot 项目后再规划；本计划不展开硬件、烤机或部署步骤。

## 6. 第一个研究实验应该怎样设计

以“视频质量筛选”为例，等一条下游训练链路稳定后使用下面的最小对照。这里不是要求你现在立即启动所有实验。

| 组别 | 数据 | 用来排除什么解释 |
|---|---|---|
| A | 固定机器人数据，无新增人类视频 | 基础参照 |
| B | 同一人视频池随机选 N 段 + 相同机器人数据 | 仅增加视频的作用 |
| C | 同一池质量筛选 N 段 + 相同机器人数据 | 质量选择是否优于随机选择 |

B/C 固定模型初始化、下游机器人数据、训练步数、采样比例、任务组成和评估协议；A 的额外计算差异要记录，不能把训练更久解释成视频贡献。若很难同时固定所有成本，明确采用“等样本”还是“等计算”的比较，不把二者混称公平。

先用少量固定任务检查方向；正式结论再增加种子和场景，报告成功数/总数与不确定性。训练 seed 控制初始化/采样，评估初始状态控制任务起点，二者都需要记录。不要拿少量 rollout 的偶然上涨作定论。

两层证据一起保留：

- **中间层：** 筛选后保留比例、人工复核正确率、轨迹/标签误差、各任务被保留多少。无真值则明确叫代理指标。
- **任务层：** 固定条件下的成功率和失败类型；接触、抓取、放置等关键阶段是否真的改善。

如果现在还接不上训练，只做中间层也有学习价值，但汇报应停在数据质量诊断。互联网视频需要有可追溯来源；先用公开研究数据的许可范围内子集，不把大规模抓取当第一个工程目标。

## 7. 哪些事情重要，哪些容易走弯路

| 容易走的弯路 | 更有效的做法 |
|---|---|
| 先学完 CV、机器人学、深度学习，再开始研究 | 对着具体接口补基础，反复用小实验检验 |
| 把所有技术都掌握作为“完整知识体系”的标准 | 公共接口熟练，一条路线深入，其他路线知道何时可用 |
| 只看 demo，或者只看方法不看实验 | 至少读数据划分、基线、一个关键消融与失败案例 |
| 认为官方训练跑通就有科研贡献 | 复现用于建立可信基线，贡献来自新问题与证据 |
| 用八张卡同时开多个方向 | 先单卡排错，把更多卡用于明确的对照和重复实验 |
| 一直读新论文，回避实验中的困难 | 新论文必须回答当前缺口，否则先归档 |
| 拼接更大的 pipeline 就认为更先进 | 固定其他因素，只改变一个环节，检查收益是否真实 |
| 等正式论文阶段才考虑评价 | 从第一段视频就保存失败样本与来源；任务评估基础现在就学 |

你可以暂缓理论全覆盖，但不能暂缓五个概念：**监督来自哪里、测试能看到什么、坐标和时间含义、基线是什么、结果支持哪一级结论。** 这五个概念比熟记几十个模型名更接近科研能力。

## 8. 有 GPT-6 和 Codex 后，工作方式应该怎样改变

不用把自己培养成“先手工实现所有模块的人”。你可以让 Codex 完成过去很耗时的仓库定位、环境隔离、数据适配、批处理、可视化、日志统计与失败排查，把自己的注意力留给问题和证据。

但有两件事不能外包掉：**定义什么结果算有意义，以及判断证据是否支持结论。** AI 写了一份逻辑顺畅的报告，不代表实验做对了。

OpenAI 对 GPT-6 Astra 的官方工作流建议强调清晰的完成条件、适用范围和必要上下文，避免累积过多固定步骤；下面是结合科研任务的具体设计，不是对模型研究能力的保证。[OpenAI 官方说明](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)

### 分工方式

| 工作 | 交给 Codex | 你必须掌握 |
|---|---|---|
| 读论文 | 找原文证据、画接口图、定位假设与消融 | 合上摘要后解释核心区别，核查关键原文 |
| 调组件 | 写调用与可视化、检查依赖与输出 | 输入条件、输出单位、三个真实失败样例 |
| 跑训练 | 准备配置、保存版本、监控异常、整理结果 | 训练监督、冻结部分、评价标准与计算预算 |
| 分析实验 | 统计、作图、核对文件与日志 | 判断混杂因素，决定结论范围与下一实验 |
| 提研究点 | 搜相近工作、提出替代解释与反例 | 选择值得研究的问题，向导师解释证据 |

不需要手写每行代码，但应能读懂关键配置，复述数据如何流动，并独立检查一个样例。审查最优先看：train/test 划分、预处理、归一化、坐标/动作、评价分母；网络内部通用层可以先交给现成实现。

### 保留最少但足够的实验记录

每次实验留一份简短记录即可：问题、数据 ID/划分、代码 commit、权重版本、完整配置/命令、随机种子、结果路径、失败、结论范围。大数据不复制进笔记，写可追溯位置。

给 Codex 一个清楚的任务边界和完成标准，让它在范围内连续修复。必要时明确允许短 smoke test，限定 GPU/数据规模；不要让它为了“跑通”默默换成假数据、换任务、取消失败样本，或更改评估规则。

同一个 AI 的解释和复查并不是独立证据。优先使用人工标注、已知几何的合成样例、官方固定示例或另一条明确的验证路径。简单可视化错误不必创建一整套测试框架。

### 可直接使用的三类请求

**读论文：**

```text
围绕这篇论文的一个训练样本，解释原始输入如何变成监督、推理时输出什么。
只教阻碍理解的术语，不讲推导。给出原文章节/图表定位，区分作者描述、
你的推断和未明确之处。随后问我三个接口或失败条件问题，先让我回答再纠正。
```

**组件实验：**

```text
用官方实现处理指定的小视频集。目标是检查一个组件，不重建整篇论文。
先确认权重、版本、输入要求与可用 GPU；保存输出数组、逐帧叠图和失败样本。
在指定数据和单卡 smoke test 范围内自行排错；缺资源要明确标记，不能用模拟
结果冒充实际输出。完成后只解释输入输出、主要失败和可复用的运行入口。
```

**研究对照：**

```text
问题是：指定的数据处理改动，在固定机器人数据预算下是否改善下游表现？
先从已有基线配置设计只改一个因素的最小对照，列出数据划分、评价协议和
计算上限。按我确认的范围执行，保留所有失败与配置；分析时优先排查
数据泄漏、额外训练量、任务分布变化和随机波动，不预设改动有效。
```

不必让 Codex 每次重读整个项目或生成长篇教程。给当前论文、当前输入、当前配置和明确的问题就够了；只有反复需要的工作流程才值得固化成技能。

## 9. 现在就可以做出的选择

你接下来不必在“读论文、学技术、跑代码”之间三选一。把它们绑在同一个对象上：

1. **选 Track2Act 作为首篇接口细读，VidBot 作为可替代的官方推理入口。** 目标是理解点/接触/轨迹怎样联系动作，不追完整机器人系统。
2. **拿一小组视频学习 mask 和点跟踪。** 暂不同时研究手重建、生成视频和大模型预训练。
3. **继续 LeRobot，达到可检查的训练与评估闭环后接 SmolVLA。** 已经完成的部分直接复用。
4. **看到具体错误后，再选显式人手数据或潜动作中的一条。** 默认从可视化与质量检查入手；需要学习预训练迁移时再做 LAWM。

与导师讨论时，优先带“一个明确接口、一组失败、一项可检验假设”，再说明需要补哪篇论文或哪个工具。这样每次阅读和实验都会推动课题收敛。

资料核对日期：2026-09-23。硬件条件来自你的描述；本次未连接实验室服务器、未测显存，也未执行训练。阶段建议按完成条件推进，所有示例对照都是待实施方案。
