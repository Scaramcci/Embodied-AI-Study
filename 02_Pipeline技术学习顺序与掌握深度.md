# Pipeline 技术学习顺序与掌握深度

**学习单位应是“这一步解决什么问题”，然后挑一个工具调用、检查失败，最后比较一个替代方案。** 不按模型名字逐个学，也不要求把所有分支接成一条流水线。

本文承接 [16 篇方法地图](人类视频到机器人操作_六篇论文方法Pipeline对照.md)。工具对应关系来自该地图与论文；以下顺序、练习和选型建议是针对你当前阶段的学习设计，不是已完成的性能比较。

## 1. 先定掌握深度

| 层级 | 掌握到什么程度 | 什么不必做 |
|---|---|---|
| A：认识接口 | 说清输入、输出、依赖、用途、一个失败条件 | 不要求安装所有候选工具 |
| B：能调用检查 | 跑官方样例，换一个输入，解释主要参数，保存中间结果并识别错误 | 不逐层研究网络 |
| C：能比较取舍 | 同一批样本比较两种方法或一项消融，记录质量、失败和成本 | 不要求提出新算法 |
| D：研究性深入 | 修改影响结论的模块，理解必要原理，并设计有区分力的实验 | 不作为每个技术的必修要求 |

你当前应把**公共接口学到 B，选定研究模块学到 C，其余分支停在 A**。所有技术都学到 C 会非常耗时，也没有必要。数学可以按需补；坐标、单位、时间和监督对象不能略过。

## 2. 按数据流学习，但允许支路提前结束

```text
相机与视频来源 → 解码/切片/时间戳 → 指令或目标与数据划分
                                      ↓
                         检测、分割、运动与几何提取
                         ├─ 点轨迹 → 深度/相机 → 物体运动 → 规划
                         ├─ 人手 → 稳定坐标/动作 → 人手预训练
                         │                         └─ 机器人重定向
                         │                             └─ 去人/渲染/遮挡合成
                         └─ 帧对/视频 → 潜动作或未来特征
                                      ↓
                         质量控制与统一样本 → 训练/微调
                                      ↓
                         离线检查 → 仿真任务评估 → 失败回查
```

先从相机概念开始，但完整 SfM/SLAM 放到真正需要稳定 3D 时。mask 之后可以调用点跟踪；去人之前可以先获得手轨迹；渲染机器人之前必须有动作映射。实际处理顺序由接口依赖决定。

## 3. 第一层：相机、视频和数据契约

### ① 相机如何把场景变成图像——B，先学薄层

**输入→输出：** 场景中的 3D 点，经相机几何投影为像素；反过来，像素配上深度和内参才可恢复相机系 3D 点。

必学：RGB/RGB-D、分辨率、帧率、视场角；内参描述成像，外参描述坐标系之间的位置与朝向；像素系、相机系、世界系、机器人基座系、末端系。内参、相机轨迹、深度不是同一种信息。

候选与取舍：已标定 RGB-D 易获得有尺度的几何，但不等于任意互联网视频；纯 RGB 更广泛，却要承担尺度、遮挡和相机运动的不确定性。相机固定时可先简化，不能把这个简化默认为移动第一视角也成立。

最小练习：用一张已知内参和深度的示例图，把几个像素抬到 3D 再投影回来；修改毫米/米、图像缩放后的内参，观察错误。只需会调用和解释，不先学多视几何证明。

验收：能解释为什么“画面里的手向右移”不一定是“世界里的手向右移”，以及为什么 resize/crop 后可能需要更新内参。

### ② 解码、采样、切片、同步——B，优先级最高

**输入→输出：** 原始视频 → 带原始时间戳和来源 ID 的帧/片段。工具先选 FFmpeg 或 OpenCV 的一种，不必同时熟练所有库。

比较：均匀抽帧简单，但会跳过短暂接触；按运动/动作边界切片更贴近任务，但容易把暂停误判为结束。VLM 切片有语义能力，也可能凭画面猜错动作边界。

必学：可变帧率、原始帧号与采样帧号、episode 边界、观测与动作的时间对应、左右手槽位、缺失与 padding mask。不要把缺失动作填零后当真实静止动作。

最小练习：同一片段用两种采样率抽帧，人工标一次接触与释放，查看事件是否消失；打印一条样本的当前帧、未来帧和动作时间区间。

验收：能从训练样本追溯原视频位置，不会跨两个 episode 配帧或生成动作块。

### ③ 任务语义、目标与数据划分——B

**输入→输出：** 视频片段 → 指令/目标图、任务标签、来源信息和训练/验证/测试归属。

比较：人工标注适合小规模可信样本；VLM 批量标注可扩展，但容易漏左右手、对象身份和动作先后；目标图省去语言描述，却要求测试时有合适目标。Track2Act 与语言条件模型不能不加说明地混为同一输入设置。

最小练习：对一组片段先自己写标签，再检查自动标签是否“说了视频里没有发生的事”。把失败或不确定片段保留为不确定，不强行补全。

**先按原视频/场景/拍摄来源划分，再切片。** 否则相邻画面可能同时进训练和测试。归一化统计也只在训练数据上拟合。若模型此前见过相关公开视频，应把无法排除的预训练重叠写成限制。

暂缓：设计复杂自动标注系统、从头训练视频理解模型。

## 4. 第二层：把画面转成可检查的视觉量

### ④ 检测与分割——先 B，再挑一项到 C

**输入→输出：** 图像/视频 + 类别、文本、框或点提示 → 框、像素 mask、对象 ID。

| 候选 | 适合 | 主要取舍 |
|---|---|---|
| Grounding DINO + SAM/SAM2 | 文本找物体，再精细分割 | 检测框错了，后续分割也会错 |
| SAM2 视频传播 | 少量提示后跟随视频对象 | 遮挡、重新入画、手物贴合时可能漂移 |
| Detectron2 等任务检测器 | 明确类别/已有权重的数据 | 框架名不等于一个通用预训练模型，需匹配任务与权重 |
| 地图中的 SAM3 | 论文指定的分割方案 | 先查对应版本接口；不因版本新就替换基线 |

首个调用选 [SAM2 官方视频示例](https://github.com/facebookresearch/sam2)，暂不横向安装所有模型。保留手、物体各自 mask，遮挡处不要强迫二者都有可靠标签。**SAM 3D 是三维重建用途，不能当同一个视频分割 API。**

最小练习：固定镜头、移动镜头、遮挡三类片段，检查全序列。人工标少量关键帧，用 mask IoU（预测与标注的交并比）辅助比较，并单独检查接触区。

验收：知道漏掉一点手为何影响去人，吞掉一点物体为何可能毁掉交互信息。

### ⑤ 2D 点跟踪与运动——B；若选择物体路线再到 C

**输入→输出：** 视频 + 起始点/起始时刻 → 各帧点位置与可见性。先用 [CoTracker 官方实现](https://github.com/facebookresearch/co-tracker)。

比较：光流描述像素的帧间运动；点跟踪追踪选定点跨多帧的位置；框跟踪跟随对象区域，不能替代接触点轨迹。CoTracker/CoTracker3 可作为首个工具，SpatialTrackerV2 在需要联合 3D 信息时再比较。

最小练习：同一物体上选多个点，对照有无 mask 限定；分别看纯相机运动与真实物体运动。记录点丢失、换点、遮挡后重现和人工标注点误差。

验收：区分“从完整视频提取已发生的轨迹”和“根据当前图预测未来轨迹”。后者才是 Track2Act 的预测任务，前者可用于生成监督。

暂缓：训练自己的跟踪网络、研究光流优化推导。

### ⑥ 深度、相机轨迹和尺度——基础 B，做 3D 路线时 C

**输入→输出：** RGB 单帧/多帧 → 深度、相机内参或相机运动；组合后才能把跨帧点放到共同坐标。

| 功能 | 地图中的候选 | 必须区分的事情 |
|---|---|---|
| 单目深度/几何 | Depth Anything、Metric3D、ZoeDepth、MoGe 系列 | 相对深度与米制深度；具体版本、训练域和输入要求 |
| 多视图结构与相机轨迹 | COLMAP/SfM、EpicFields | 主要借静态场景约束，动态手物会干扰 |
| 动态视频相机恢复 | MegaSAM | 不因输出了轨迹就默认尺度和动态分离准确 |
| 内参估计 | DroidCalib、DeepCalib 等 | 内参估计与世界系轨迹恢复是不同环节 |

首轮只选一个深度工具，检查输出定义，再使用论文配套相机结果；不先从零搭 SLAM。不能默认所有同名系列的新旧版本都输出相同单位。

最小练习：先用有真实深度/相机参数的样例检查坐标变换，再替换为预测量。把每帧静态背景点变回共同坐标，观察漂移；这是诊断，不是完整精度证明。

验收：知道单目“看起来立体”不等于可恢复真实尺寸；只靠重投影小误差也可能藏着整体尺度错误。

### ⑦ 人手关键点、MANO 与时序重建——先 A；进入手路线到 B/C

**输入→输出：** 图像/视频 → 2D/3D 关节、MANO 参数、手腕位姿或相机参数。MANO 是参数化手模型，不是机器人控制器。

顺序：先识别 21 点和左右手 → 调用 [HaMeR 样例](https://github.com/geopavlakos/hamer) → 再读 HaWoR 的视频与相机处理 → 需要时比较 WiLoR + 时序优化。MANO 模型文件的获取条件需单独检查。

取舍：单帧工具易检查，但帧间抖动和全局运动不稳；视频方法利用时序，工程和相机依赖更复杂。二维叠图很好看，仍可能有深度、尺度、手腕朝向错误。

最小练习：同一片段保存骨架叠图和腕部轨迹，标出遮挡帧、左右手交换、异常跳变。对输入水平翻转后检查左右手含义，不能只比较图像外观。

验收：能分清 MANO 姿态参数、关节坐标和世界系手轨迹，知道一个腕点不足以确定完整朝向。无真实 3D 标注时，仅报告二维误差和稳定性代理指标。

### ⑧ 物体、接触与可供性——A，按研究接口提升

**输入→输出：** 物体 mask/视频/深度 → 物体网格、6D 位姿、3D 点流、接触点或可交互区域。

先比较表示，再选模型：刚体 6D 位姿紧凑，但不描述物体形变；点流更一般，但噪声和跟踪对应更难；接触点+路径适合表达意图，但不能说明抓握稳定。SAM 3D、6D tracker、TSDF 各承担形状、姿态、场景几何任务，不是三个可以直接互换的算法。

最小练习：用带已知运动的仿真物体对比“同一个刚体变换能否解释所有点”；随后在真实片段看残差。接触判断先用可见事件做人工检查，不把距离阈值写成接触力真值。

暂缓：软体物理、高精度摩擦、完整灵巧手手物重建。若选 Do as I Do，再系统提升这一环到 C。

## 5. 第三层：从视觉量变成机器人相关监督

### ⑨ 动作表示、重定向、平滑与可达性——接口 B，优化细节 A

**输入→输出：** 人手/物体轨迹 → 机器人末端目标、夹爪开口或关节目标；过程中可能有 IK 和轨迹优化。

先学：绝对位置/相对位移、角度/弧度、四元数顺序、TCP 定义、状态与动作、FK（关节→末端）/IK（末端→关节）。这是接口知识，不要求先推雅可比。

| 候选 | 优点 | 损失或代价 |
|---|---|---|
| 手指关键点→平行夹爪 | 易形成位置、方向、开口标签 | 丢失多指接触，关键点噪声可能放大成朝向错误 |
| 人手表示→后训练动作头 | 可保留人手信息供模型学习 | 迁移效果依赖机器人数据，不能直接下发 MANO |
| 几何 IK/轨迹优化 | 可显式检查关节限制、可达性 | 找到 IK 解不保证避碰或抓稳 |
| 物理仿真重定向 | 可纳入接触和动力学 | 对形状、质量、摩擦和仿真质量更敏感 |

最小练习：先用干净合成轨迹检查坐标和 IK，再接预测关键点。比较平滑前后抖动与动作时序；Savitzky–Golay 可平滑位置，旋转插值要按旋转表示处理，不能随意对欧拉角跨 ±π 求平均。

验收：分别统计无效输入、求解失败、越限、碰撞、动作跳变，不用一个平均误差掩盖失败样本。

### ⑩ 去人、机器人渲染与遮挡合成——机器人化路线 B/C，其余 A

**输入→输出：** 原视频 + 人体 mask → 修复背景；再加机器人轨迹、模型、相机参数 → 合成视频。

顺序是：先检查 mask → E2FGVI/ProPainter 去人 → 检查被遮挡物体是否被补错 → 渲染一种机器人 → 最后处理深度遮挡。不要一开始追 15 种形态。

比较：2D overlay 简单、易验证，但前后遮挡容易错；深度合成更合理，却继承场景深度误差；修复模型补出来的隐藏区域是估计，不是恢复了真实观测。生成式替换可更自然，但运动和动作标签对应关系仍需独立检查。

最小练习：同片段保存原图、mask、去人、机器人层、最终合成五路结果。评价接触对象是否保留、遮挡顺序、动作与画面是否同步，不能只比“逼真程度”。

验收：能定位错误来自手轨迹、mask、修复、相机渲染还是遮挡；以后做下游对照时，编辑组与未编辑组必须来自相同原始片段。

### ⑪ 潜动作、IDM 与未来表征——先 A；选择无标签路线到 B/C

这是一条**替代/补充显式动作提取的分支**，不必先完成三维重建。

| 方法类 | 输入→输出 | 取舍 |
|---|---|---|
| LAPA 量化 | 前后帧→潜动作 token | 可用无动作视频，但 token 无物理单位，也可能编码相机运动 |
| LAWM 世界模型监督 | 当前条件→潜动作→预测未来 | 可联合训练；未来重建好不一定意味着控制好 |
| JALA 式对齐 | 边界帧表征与策略隐藏态对齐 | 可结合显式标签；需防表征塌缩并检查混合监督 |
| 真实动作 IDM | 前后观测→动作标签 | 需要有动作数据训练；在生成视频上的误差可能变化 |
| DINO/V-JEPA 等视觉特征 | 图像/视频→特征向量 | 可保留语义变化，但不是天然的动作编码 |

最小练习：选“背景变、物体不动”和“背景不变、物体动”的受控样例，观察表示变化；这只能定位混淆，不能单凭聚类图宣布学到了动作。

暂缓：量化理论、RSSM 概率推导、EMA 理论。先说清哪部分看了未来，推理时是否还能访问它。

### ⑫ 质量过滤与训练样本统一——B；适合作为首个 C 级研究候选

**输入→输出：** 各种感知/伪动作结果 → 含质量标记、单位、坐标和有效维 mask 的训练样本。

比较：硬过滤简单但可能删掉困难任务；置信度加权保留样本却依赖分数可靠性；按质量分配不同训练目标更灵活，也更难隔离贡献。可对照 Ego2Robot 的清洗、Qwen 的统一字段、LDA 的质量分工。

最小练习：人工审查一小组，检查模型置信度是否真的对应正确性。只改一个过滤条件，报告保留比例、任务组成、失败类型；比较同样数量的随机子集，避免把样本数量变化误当质量收益。

必须会：训练集归一化/推理反归一化、旋转和位置分别处理、动作 chunk、缺失 mask、相机视角映射。格式转换成功不等于语义对齐成功。

## 6. 第四层：训练与评估——必须提前接触，不等 CV 全学完

### ⑬ 模仿学习与 VLA——训练接口 B，架构细节 A

先区分：模仿学习用示范监督动作；VLA 把视觉、语言与动作联系起来；ACT、Diffusion Policy 可以是动作策略，但并非任何配置都属于基础 VLA。

调用顺序：加载一条机器人轨迹 → 已有策略推理 → 检查动作与归一化 → 单批次训练 → 小数据训练与 checkpoint 恢复 → 仿真评估 → VLA 微调。

模型名对应的最小知识：

| 技术 | 现在需懂 | 暂缓 |
|---|---|---|
| CLIP/SigLIP/DINO、VLM 底座 | 输入尺寸、输出特征、冻结/训练、是否需要语言 | 预训练目标的详细推导 |
| Perceiver/Transformer/DiT | 融合哪些输入、输出哪类序列、mask 的作用 | 每个注意力层源码 |
| 直接回归/离散 token | 输出连续值或类别，如何变回动作 | 全套概率建模 |
| diffusion/flow matching | 训练预测对象、条件、采样步骤和动作块 | SDE/ODE 推导 |
| LoRA/冻结/全参数微调 | 更新哪些参数，显存与适配能力的取舍 | 低秩理论 |
| MoT/MoF、GRQ、统一动作槽位 | 分别是模型组织、量化、数据表示问题 | 在第一项目中重新实现 |

建议使用 [SmolVLA 官方接口](https://huggingface.co/docs/lerobot/smolvla)承接 LeRobot，具体训练与仿真路线见第三份文档。模型选择服务于学习闭环，不以参数量为进步标准。

### ⑭ 离线检查与仿真评估——基础 B，研究时 C

分三个层次：组件结果是否正确；伪标签是否可信；用这些标签训练是否改善任务表现。三者不能替代。

先掌握：训练/验证/测试区别、同分布与跨场景、成功条件、分母、随机种子、失败视频。比较方法时固定任务与初始状态，分别保留基础设施失败和策略失败。

没有真值时，重投影、抖动、可达率等是代理指标；有真值才报告对应精度。最终“有助于机器人学习”的结论应落到下游任务，不能仅凭视频好看、loss 下降或伪标签更多。

### ⑮ 视频生成与复杂规划——目前 A，最后扩展

Wan 等视频模型、DreamGen 式伪动作回填、Dream2Flow 式目标提取，以及 AnyGrasp、PyRoki、MPPI、SAC，先各写一句输入输出即可。它们分别涉及生成、抓取候选、运动优化或强化学习，并非一个必学软件包。

只有当问题确实需要新增任务视频、物体目标或接触规划时，再挑一项到 B。先使用固定的小量生成结果隔离下游误差，不同时更换生成器、跟踪器和控制器。

## 7. 你现在真正要执行的顺序

1. **①②③的基础接口**：相机薄层、时间戳、数据来源和标签；用一个样本说明白。
2. **④⑤**：一段短视频的 mask 与点轨迹，做逐帧检查。
3. **⑥的最小几何 + Track2Act/VidBot 推理**：理解中间输出，不追整套训练。
4. **同时接⑬⑭**：通过你正在做的 LeRobot，建立训练和评估参照。这里不必等⑦至⑫全部完成。
5. **按选择只深入一支**：手路线⑦→⑨→⑩→⑫；物体路线⑥→⑧→⑨；潜动作路线⑪→⑫→⑬。
6. 出现具体误差之后，才比较第二个模型、深入 C 级或补必要原理。

每次小实验只保存必要产物：来源与配置、输入/输出、中间可视化、失败样本、短结论。让 Codex 负责调用脚本与批处理；你负责解释三个失败样本，并判断下一步该换工具还是改数据。这比攒几十份互不相连的技术笔记更能建立知识体系。

组件仓库入口于 2026-09-23 核实；本次未运行性能或显存测试。对尚未实测的工具优劣，均按接口与方法假设描述，不构成同数据 benchmark。
