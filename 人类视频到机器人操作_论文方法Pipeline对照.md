# 从互联网视频到机器人学习：16 篇论文的具身方法地图

## 0. 先抓住问题：视频到底提供了什么？

导师的命题可以写成：给定大量视频，其中没有机器人能直接执行的动作标签，如何从中获得对训练或控制机器人有用的监督？一个视频至少可能提供四类东西：

1. **可见的物理变化**：物体去了哪里、哪些点怎样移动、门开了多少。无需知道人手每个关节。
2. **人类动作**：手腕、手指、手—物接触、物体姿态。需要手/物体三维重建，或者已有追踪标注。
3. **隐含动作**：不给动作一个物理单位，而从前后帧变化学习“潜动作”或未来状态表征。
4. **合成经验**：让视频生成模型“想象”新的机器人视频，再设法给它补上伪动作，作为训练数据。

所以“处理视频 → 训练 VLA”不是唯一终点。有些论文只预测中间目标并交给规划器，有些学习人手 VLA，有些把视频改成机器人数据，有些只用视频做辅助的未来预测。它们应在**中间表示与训练目标**处分叉，而不是硬塞进一条管线。

### 最小术语表

| 词               | 在本文里的意思                                              | 不要误解成                 |
| ---------------- | ----------------------------------------------------------- | -------------------------- |
| **观测** $o_t$   | 某时刻的图像/深度/机器人状态                                | 机器人动作                 |
| **动作** $a_t$   | 实际控制接口，如关节角、末端位姿、夹爪开口                  | 任何视频里的“运动”         |
| **伪动作**       | 从视频或模型推断的动作监督                                  | 必然可在真实机器人直接执行 |
| **潜动作** $z_t$ | 编码前后帧变化的向量或 token                                | 具有明确米、弧度含义的动作 |
| **可供性**       | “哪里能接触、怎样运动”的交互线索                            | 完整控制策略               |
| **VLM / VLA**    | 视觉语言模型 / 带动作生成能力的视觉语言动作模型             | 任何用了语言的机器人程序   |
| **IDM / FDM**    | 逆动力学：前后观测→动作；前向动力学：当前观测+动作→未来观测 | 物理模拟器必然真实         |
| **重定向**       | 人手动作→特定机器人动作的映射                               | 仅仅改变图像外观           |
| **对齐**         | 可能是时间、坐标、视觉域、动作槽位、语义或未来表征对齐      | 某一种统一算法             |

## 1. 总地图：五条主路线，而非一条流水线

```text
视频 +（可选）指令 +（可选）真实机器人数据
│
├─ A. 显式几何/物体运动 → 机器人规划
│  ├─ VidBot：3D 接触点 + 手中心交互轨迹
│  ├─ Track2Act：2D 点轨迹 → 3D 物体刚体变换 → EEF 粗计划
│  └─ Dream2Flow：生成的未来视频 → 3D 物体 flow → 轨迹优化/RL
│
├─ B. 显式人手/手物重建 → 人手模型或机器人化数据
│  ├─ VITRA：移动相机 + MANO 双手 → 人手 VLA → 灵巧手微调
│  ├─ Being-H0：MANO → 离散手动作 token → 机器人后训练
│  ├─ Being-H0.5：人手/多机器人统一动作槽位 → MoT/MoF VLA
│  ├─ Masquerade：HaMeR → 夹爪 + 去人/叠机器人 → 视觉预训练
│  ├─ Ego2Robot：手→夹爪 + 15 形态渲染 → 合成 VLA 数据
│  ├─ Qwen-RobotManip：H2R + 真实数据统一 → 多形态 VLA/VLM
│  └─ Do as I Do：手 + 物体 4D 重建 → 物理仿真灵巧手重定向
│
├─ C. 无显式动作标签 → 潜动作预训练
│  ├─ LAPA：前后帧 → VQ 潜动作 → VLA 预训练 → 机器人微调
│  ├─ LAWM：策略猜潜动作 → 世界模型重建未来帧 → 微调策略
│  └─ JALA：逆动力学潜动作 ↔ 预测隐藏态；有手标签时加显式监督
│
├─ D. 视频世界模型造训练数据
│  └─ DreamGen：机器人视频模型 → 新视频 → IDM/LAPA 伪动作 → 策略训练
│
├─ E. 当前观测 + 未来预测的联合模型
│  ├─ LDA-1B：动作/未来 DINO 特征共同学习，按数据质量分配任务
│  └─ Being-H0.7：未来帧只在训练期教 latent queries，推理期直接出动作
│
└─ 交叉关系
   ├─ Dream2Flow 也用视频生成，但目的是“在线得到物体目标”，不是造训练集
   ├─ DreamGen 可用 LAPA 给生成视频标潜动作
   ├─ LDA-1B 与 LAWM 都学未来，但一个多任务 DINO latent，一个 RSSM 像素预测
   ├─ JALA 同时使用有手标签与无手标签视频，连接 B 与 C
   └─ Being-H0 → H0.5 → H0.7 是相关系列，不是同一条数据处理流水线的三个必经步骤
```

### 先用“输出是什么”给论文归位

| 输出或主要中间表示                   | 论文                                               | 是否要求从视频得到真实机器人动作 |
| ------------------------------------ | -------------------------------------------------- | -------------------------------- |
| 3D 接触、点轨迹、物体 flow           | VidBot、Track2Act、Dream2Flow                      | 否；后续规划/控制另做            |
| MANO 人手动作                        | VITRA、Being-H0、Being-H0.5                        | 不一定；微调/统一空间负责迁移    |
| 机器人可执行或近似可执行的重定向轨迹 | Masquerade、Ego2Robot、Qwen-RobotManip、Do as I Do | 是，但有不同程度的 IK/物理验证   |
| 非物理单位的潜动作                   | LAPA、LAWM、JALA                                   | 预训练不要求；最终微调需要       |
| 生成视频加伪动作                     | DreamGen                                           | 需要 IDM 或潜动作模型补标签      |
| 未来状态/未来视觉 latent             | LDA-1B、Being-H0.7                                 | 与动作共同训练，推理方式各异     |

## 2. 共同基础：看任何一篇论文都先问这七个问题

以下是**阅读框架**，不是所有论文都必须依次做完：

| 问题                  | 要记录的具体内容                                                                   | 为什么重要                                   |
| --------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------- |
| 数据从哪来？          | 第一/第三人称，原始人类/真实机器人/仿真/生成视频；是否有语言、深度、动作、相机标定 | 决定后面技术能不能用                         |
| 如何切片、标注任务？  | 原始 clip、原子动作、子任务、图像目标或文本目标                                    | 长视频可能包含多个动作，标签粒度决定训练目标 |
| 视觉提取什么？        | 掩码、关键点、MANO、物体 6D、2D tracks、深度、相机位姿                             | 决定“动作”最终的精度和含义                   |
| 输出在哪个坐标系？    | 图像像素、相机、世界、机器人基座、末端局部；绝对/相对                              | 不统一就不能混数据或下发控制                 |
| 怎样跨人/机器人形态？ | 不跨（物体目标）、确定性手→夹爪、物理重定向、统一槽位、下游微调                    | 是这个方向的核心分歧                         |
| 训练目标是什么？      | 轨迹预测、动作模仿、帧预测、潜动作、流匹配、VLM 辅助任务                           | 不能把所有训练都叫“VLA 预训练”               |
| 测试时还需要什么？    | RGB-D、标定、目标图像、语言、真实数据、模拟器、在线视频生成                        | 关系到技术能否落地                           |

一个非常实用的**最小数据契约**是：`视频帧 + 帧时间戳 + episode 边界 + 指令/目标 + 相机内外参（如有）+ 状态/动作（如有）+ 单位和坐标系说明 + 质量标记`。在做 LeRobot 项目时，优先弄清这个契约，比先背模型名更重要。

## 3. 路线 A：物体与交互几何——让视频告诉机器人“物体应如何变”

这一路线的共同思想：**机器人手和人的手可以不同，但同一任务的物体变化仍有可迁移性。** 三篇文章的目标又不完全一样。

### A1. VidBot：RGB 人类视频 → 3D 可供性

```text
EPIC-KITCHENS RGB + 语言
→ SfM/EpicFields 得到相机轨迹、稀疏地标
→ 单目深度（ZoeDepth / Metric3D v2 / Depth Anything）
→ hand-object detector + SAM/细粒度手物分割
→ E2FGVI 去手；在静态区域联合优化尺度、相机位姿与深度
→ 手中心 3D 轨迹；取初始接触点 c、末端目标点 g、交互路径 τ
→ 训练粗阶段 contact/goal heatmap + 细阶段 3D 轨迹扩散模型
→ 测试时用 TSDF 几何代价引导扩散轨迹，再交给机器人规划
```

- 粗阶段：语言用 **CLIP**，图像/物体/框/语言用 **Perceiver** 融合；contact 与 goal 在像素热图中预测，再由深度抬到 3D。
- 细阶段：**1D U-Net diffusion** 生成轨迹；**TSDF + 3D U-Net** 提供碰撞几何；测试时加入多目标、碰撞和接触法向代价。
- 关键边界：它**不恢复完整手指骨架，也不是直接输出机器人关节的 VLA**。训练视频可为纯 RGB，但部署需要 RGB-D 或深度估计。
- 适合学习的问题：何为 affordance？为什么“接触点/目标点/路径”可作为中间接口？单目深度和 SfM 各解决什么？

### A2. Track2Act：网页视频 → 2D 点轨迹 → 3D 物体刚体运动

```text
人/机器人网页视频
→ CoTracker 生成 2D 点轨迹监督（第一帧为起点，末帧为目标图像）
→ DiT 扩散模型学习：起始图 + 目标图 + 起始点 → 未来 2D tracks
→ 部署时由初始 RGB-D 把物体上活动点抬到 3D
→ 过滤静止背景/离群点，RANSAC + 重投影优化/PnP 求逐时刻刚体变换
→ 刚体变换作用于初始 EEF 姿态，得到开环粗轨迹
→ 少量真实机器人示范训练 residual policy，闭环修正每步动作
```

- 目标是**目标图像**，不以自然语言为主要接口。
- 训练网络预测的是点轨迹，不生成完整视频；部署时才通过深度和相机内参转 3D。
- **RANSAC** 处理点跟踪离群值；对象运动近似刚体，单次处理一个主要被操纵对象。粗计划的抓取点含启发式，残差策略负责纠错。
- 适合学习的问题：光流、点跟踪和目标图像各是什么？为什么需要初始深度？什么是开环计划与闭环残差？

### A3. Dream2Flow：生成视频 → 3D 物体 flow → 在线规划

```text
当前机器人场景 RGB-D + 文本任务
→ 现成图生视频模型生成“人/物体完成任务”的未来视频
→ Grounding DINO 物体框 + SAM2 物体掩码
→ CoTracker3 跟踪物体上的 2D 点
→ SpatialTrackerV2 估计生成视频逐帧深度；首帧对齐真实 RGB-D 的尺度/偏移
→ 点轨迹 + 深度 + 相机参数 → 机器人坐标中的 3D object flow
→ 用轨迹优化、推送动力学模型或 RL 让机器人跟踪物体 flow
```

- 视频生成模型提出“**物体应该怎么变**”，不需要生成精确机器人手动作。论文刻意从初始图像中去掉机器人，避免未针对机器人微调的视频模型产生不可信的机械臂运动。
- 真实机器人抓取候选用 **AnyGrasp**；生成视频中的 **HaMeR** 拇指位置辅助选择应抓的物体部位；EEF 轨迹优化使用 **PyRoki**。仿真门操作可用 **SAC** 以 flow 作奖励训练策略。
- 关键边界：这是**推理时**用生成视频定义目标，**不是** DreamGen 那种离线生成大批训练数据；生成视频的幻觉、物体变形和跟踪丢失会直接影响规划。
- 适合学习的问题：3D flow 与刚体 6D 姿态何时等价、何时不等价？为什么布、颗粒、关节物体需要比刚体变换更一般的表示？

### A 路线对照

|                   | VidBot                   | Track2Act                              | Dream2Flow                 |
| ----------------- | ------------------------ | -------------------------------------- | -------------------------- |
| 来源              | 真实人类视频             | 真实人/机器人网页视频                  | **生成**的任务未来视频     |
| 训练/测试核心表示 | 3D 接触点和手中心路径    | 预测 2D 点轨迹，测试时拟合 3D 刚体变换 | 物体上点的 3D flow         |
| 目标接口          | 语言                     | 目标图像                               | 语言 + 当前 RGB-D          |
| 需要深度的位置    | 视频 3D 标签与部署       | 部署起始帧                             | 当前首帧与生成视频尺度对齐 |
| 跨具身方式        | 手路径抽象为可供性       | 物体运动转 EEF 粗轨迹                  | 物体 flow 作为优化/RL 目标 |
| 主要局限          | 3D 伪标签受 SfM/深度限制 | 刚体、单主要对象、残差示范仍必要       | 生成质量与物体跟踪决定效果 |

## 4. 路线 B：显式手/物重建——什么时候保留人手，什么时候把人换成机器人？

这一路线先在数据中获得有物理意义的手/机器人动作。但有两种截然不同的出口：**保留人手表示训练模型**，或**先把人手转为机器人动作/画面**。

### B1. 保留人手：VITRA、Being-H 系列

#### VITRA：先把移动相机和无结构视频变成 VLA episode

```text
Ego4D / EPIC-KITCHENS / EgoExo4D / Something-Something-V2
→ 背景光流判断相机静止/运动
→ DroidCalib（运动相机内参）或 MoGe-2 + DeepCalib（静态）
→ HaWoR 双手 MANO + 修改版 MegaSAM 相机轨迹 + MoGe-2 尺度
→ 世界系手腕轨迹平滑；左右手速度极小值独立切原子动作
→ 每段 8 帧叠加未来掌心轨迹，GPT-4.1 生指令
→ 当前图 + FoV + 指令 + 当前手状态 → 未来双手连续动作
→ PaliGemma-2 / SigLIP / Gemma-2 + DiT 扩散动作专家
→ 真实灵巧手数据微调，MANO 与机器人关节按拓扑映射并 mask
```

- 不是“先去手再训练”。它把两只人手当两个末端执行器；单手缺失槽位用 mask。**因果动作去噪**避免短片段尾部补零影响之前的有效动作。
- 它很适合学习：为什么相机内参、相机轨迹、世界坐标、当前相机坐标要分清？为什么视频任务切分和语言标注与 3D 重建同样重要？

#### Being-H0：连续 MANO → 离散手动作 token → 机器人后训练

```text
MoCap / VR / RGB 视频
→ 已有手标签直接转 MANO；只有 3D 点则拟合 MANO；无标签用 HaMeR
→ 弱透视相机对齐 + 视角/深度分布增强
→ 最长约 10 秒片段 + 1 秒窗口
→ Gemini-2.5-Flash-Lite 分层标注；Gemini-2.5-Pro 扩展指令模板
→ MANO-D162 + 腕/手指分开的 Part-Level GRQ 离散动作 token
→ InternVL3/InternViT 统一自回归视觉、文字、动作任务
→ 机器人本体状态投影 + learnable action queries + MLP 后训练输出连续动作
```

- 三类预训练任务：视觉/指令→手动作，手动作/视觉→文字，历史→未来手动作。
- **弱透视对齐不是完整动态相机 SLAM**。论文并未像 VITRA 那样主攻剧烈移动相机的世界系恢复；也未显式恢复物体 6D 或接触力。
- 机器人动作不是把 GRQ 手 token 逐 token 直接翻译给电机，而是通过后训练的控制头学习。

#### Being-H0.5：人手从“预训练目标”变成跨机器人共同动作语言

```text
UniHand-2.0：人类第一视角 + 多机器人 + 视觉语言数据
→ 野外人手用 HaWoR 估 MANO/相机；Gemini-2.5 双层标注
→ 置信度/DBA/腕部抖动过滤、非操作片段排除、左右镜像去偏
→ MANO 腕部放入共享 EEF 槽，手指放 fine-manipulation 槽
→ 不同机器人的状态/动作也映射到 Unified State-Action Space
→ 连续动作块 flow matching + 离散手 motion token 的 masked prediction
→ InternVL3.5 初始化的 Mixture-of-Transformers（MoT）
→ Mixture-of-Flow（MoF）：共享底层 + 路由的形态/任务专门动作专家
→ 特定形态后训练；Manifold-Preserving Gating + Universal Async Chunking 部署
```

- 与 H0 的关键变化：**人手、平行夹爪、灵巧手、多机器人进入统一语义槽位**，且连续动作和离散手动作一起监督；它不是“把人渲染成机器人”。
- 它规定 EEF 相对位移在统一世界系、旋转用 axis-angle、关节用绝对弧度；不要与 **Qwen-RobotManip 的相机系 EEF 增量**混同。
- **UniCraftor** 是额外的便携数据采集系统，不是从互联网 RGB 视频自动恢复三维的必需步骤。
- MPG/UAC 主要属于后训练与实时控制稳定性，作为你当前“数据处理地图”的外围模块先认识即可。

#### Being-H0.7：在 H0.5 底座上增加“未来有用信息”的 latent 推理

```text
UniHand-2.0 格式的人/机器人轨迹
→ 当前指令、历史图像、状态 + learnable latent queries + 带噪动作
→ 训练期第二支：未来图像 → V-JEPA2.1/frozen ViT + Perceiver → future embeddings
→ prior（只看当前）与 posterior（看未来）在 latent query 隐藏态上对齐
→ 两支都做 flow-matching 动作损失；加 norm/rank 防塌缩正则
→ 推理时删除 posterior，不生成未来像素，只用 prior 直接出动作
```

- **后验支路的未来图像只在训练时可见**；推理期不能假设未来真实图像可用。
- 这里的“world”是动作相关 latent 的未来信息，不是完整视频生成器。其 **MoT 双分支 attention mask**让两支共享当前上下文又避免直接偷看彼此。
- 论文侧重 VLA 模型结构和未来监督，而不是提出新的手部分割/去手 CV 管线。

### B2. 把人变成机器人：Masquerade、Ego2Robot、Qwen-RobotManip

这三篇共享一个外观和动作的双重对齐方向，但目标差别很大。

#### Masquerade：粗糙但有效的机器人化视觉数据

```text
EPIC-KITCHENS 手标注 + 活动文本
→ HaMeR 每手 21 个关键点
→ Phantom 方法映射到双 Kinova EEF 位姿/夹爪开合
→ Detectron2 + SAM2 分割人体；E2FGVI 去人
→ 已知相机参数下渲染双臂机器人并 2D overlay
→ Homography 把未来关键点 warp 回当前相机视角，过滤过大相机移动
→ ViT-Base + DistilBERT/FiLM 预测未来 2D 机器人关键点
→ 少量真实机器人数据训练 Diffusion Policy，继续保留人类视频辅助损失
```

- **没有可靠绝对 3D 深度**，所以主要用未来 2D waypoint 预训练视觉表示；机器人叠加的遮挡顺序可能出错。
- `co-training` 指微调时继续做“编辑后人类视频→未来 2D 点”辅助任务；不是通用基础 VLA 的大规模动作联合训练。

#### Ego2Robot：可扩展的 15 形态 H2R 数据工厂

```text
路径 A：ANT / EgoDex / VITRA / EgoVerse 的已有手标签
路径 B：纯 Ego Play 视频 → WiLoR + SAM3 检验 + DynHaMR 时序优化；Qwen3.5 切子任务
→ 拇指尖 + (0.7 食指尖 + 0.3 中指尖) 得虚拟夹爪/TCP/开口/方向
→ Savitzky–Golay 平滑位置和开口，SLERP 平滑旋转；按来源调整动作速度
→ SAM3(person) 分割 + ProPainter 去人
→ 搜索可达机器人基座 + MuJoCo IK + 15 形态渲染
→ Depth Anything V3 场景深度和机器人深度做遮挡合成
→ L1 物理/IK、L2 统计、L3 Qwen3.5 语义一致性清洗
→ 合成机器人视频 + 相机系相对 EEF 动作 + 指令
→ 与真实机器人数据混合预训练 Qwen3.5/DiT flow-matching VLA
```

- 方法创新重心是**数据合成、重定向、清洗与规模化**；不应只把它记为一种 VLA 架构。
- 人手到平行夹爪是有损压缩，细手指动作无法保留。场景深度和修复伪影会影响视觉真实性。

#### Qwen-RobotManip：H2R 只是输入之一；核心是异构数据的三层对齐

```text
开源真实机器人数据 + 原始人手 + H2R 15 形态合成 + 视觉语言数据
→ H2R 使用 MANO→夹爪、SAM3、ProPainter、MuJoCo IK/渲染、Depth Anything V3
→ 真实机器人数据做突变、状态动作时间、FK、TCP、坐标、视觉与语义一致性清洗
→ UnifiedSpace：80 维语义状态动作模板 + 有效维 mask
→ UnifiedEEF：参考相机系相对末端增量 + CaPE + 相机内参 patch 编码
→ 行为对齐：结构化 embodiment/speed/fps/view 提示 + 历史观测/状态/动作上下文
→ Qwen3.5-4B VLM + DiT flow-matching 动作专家
→ VLA 操作数据和 VLM 数据双流联合训练，再做目标域后训练
```

- **表示对齐**是不同机器人字段能放进同一模板；**运动对齐**是相似视觉运动对应相似动作数值；**行为对齐**是用历史反映机器人当前形态与执行风格。
- CaPE 和相机系动作需要理解并尽量获得相机几何；“相机系”不等于“完全不需要标定”。
- 额外 VLM 流含 ECoT、第一人称视频理解、2D 轨迹预测等任务；ECoT 使用 Qwen3.6-Plus 辅助标注。它让模型在大量动作训练中尽量不忘记语言和空间理解。
- H2R 数据生成与 Ego2Robot 高度相关，但 Qwen-RobotManip 的范围还包括跨来源真实机器人清洗、统一动作空间、多视角模型设计和 VLM 联合训练。

### B3. Do as I Do：不是“人手压成夹爪”，而是手—物体共同重建 + 物理重定向

```text
任意 ego/exo 单目 RGB 人类视频（也可为生成视频）
→ SAM3 同时分割手/物体
→ HaWoR 跟踪三维手；MoGe 估深度与相机内参
→ SAM 3D 从单帧生成物体 3D mesh
→ 对 SAM 3D 做 guided diffusion：固定物体形状，逐帧跟踪 6D 物体姿态
→ 点轨迹估计旋转速度，调节姿态引导；候选姿态聚类 + mask-IoU 选取
→ 手/物体尺度与位姿对齐，GeoCalib 对齐重力
→ 物理仿真里的 MPPI 风格采样优化，将手—物轨迹重定向到多指灵巧手
→ warmup、随机力扰动、接触/离手阶段的 transition reward 增强鲁棒性
→ 输出机器人可播放的灵巧手/机械臂轨迹
```

- 它比 Ego2Robot 更强调**物体 3D 形状与姿态、接触物理、灵巧手可执行性**，而不是大规模平行夹爪视觉合成。
- **SAM3（分割）≠ SAM 3D（生成物体 3D）**；名字相近，但在 pipeline 中是两种不同工具。
- `guided diffusion` 在这里用于**物体姿态跟踪**，不是 VidBot 的轨迹扩散，也不是 DreamGen 的视频生成。
- 主要假设与难点：针对**刚性物体**的形状/姿态追踪；视频质量、遮挡、手物尺度偏差和仿真物理会影响最终重定向。其输出是数据生成/回放，不是直接训练好的通用 VLA。

### B 路线里“对齐”到底指什么？

| 对齐问题                 | 代表方法                  | 具体手段                                                   |
| ------------------------ | ------------------------- | ---------------------------------------------------------- |
| 动态相机与世界轨迹       | VITRA                     | 内参 + MegaSAM 相机轨迹 + MoGe-2 尺度 + 世界系手轨迹重投影 |
| 多来源图像投影差异       | Being-H0                  | 弱透视内参规范化、深度缩放与同步图像变换                   |
| 人手与机器人控制形态     | VITRA/H0                  | 保留 MANO，让机器人微调/后训练学习差异                     |
| 人手与平行夹爪控制形态   | Masquerade/Ego2Robot/Qwen | 手关键点→EEF 位姿/夹爪宽度；IK 与渲染                      |
| 人手与多指灵巧手物理行为 | Do as I Do                | 手—物 4D 重建后在仿真中动态重定向                          |
| 多机器人状态字段         | Being-H0.5/Qwen           | 统一语义槽位、缺失维度掩码、专门专家或提示                 |
| 多相机视觉与 EEF 运动    | Qwen                      | 相机系相对增量 + CaPE/内参编码                             |

## 5. 路线 C：没有可靠 3D 手标签，能否仍从视频学“动作”？

这一路线用视觉变化作监督，但**潜动作不天然有物理单位或可执行性**。它们通常仍需要少量机器人有标签数据来完成控制适配。

### C1. LAPA：最清楚的三阶段基线

```text
当前帧 + 未来帧
→ action quantization model：encoder 得 z，decoder 用当前帧+z 重建未来帧
→ VQ-VAE/NSVQ 把 z 变为离散潜动作 token
→ 给无动作标签的人/机器人视频批量打潜动作伪标签
→ 预训练 VLM：当前图 + 任务文本 → 预测潜动作 token
→ 少量真实机器人动作数据微调：替换潜动作头为真实动作头
```

- 优点：预训练无需真实机器人动作；容易利用大规模无标签视频；明确展示“先学视觉变化，再映射机器人动作”。
- 局限：未来帧重建会把**相机移动、背景变化**也编码成潜动作；潜动作不直接可执行；原论文也指出在某些同域设置下不如真实动作预训练。
- 别把这里的“latent action”理解成只属于人手，它可从人/机器人视频中学习。

### C2. LAWM：策略与世界模型端到端互相约束

```text
当前图 + 指令
→ 模仿策略先猜一段连续潜动作
→ RSSM/DreamerV3 风格世界模型：当前帧 + 潜动作，逐步预测未来帧
→ 未来帧重建 MSE + RSSM 的 KL 约束联合训练策略和世界模型
→ 下游有标签机器人微调时丢掉世界模型，只保留策略并改动作输出层
```

- 与 LAPA 最大差别：**不先训练独立 VQ 量化器再给视频离线贴 token 标签**，而是让策略产生的潜动作通过世界模型的未来帧预测获得自监督。
- 框架声称 model-agnostic，论文实现包含 **BAKU / Diffusion Policy** 策略与 **DreamerV3 RSSM** 世界模型。
- 局限：未来图像重建仍会花容量处理与动作无关的颜色/背景；未来帧模型误差可反过来影响学到的潜动作。

### C3. JALA：有标签手数据 + 无标签野外视频一起学

```text
UniHand-Mix：实验室高精度手数据 + Ego4D 野外视频
→ 野外视频 WiLoR 看手；Gemini-2.5-Flash 判断是否真正操作并标语言
→ 可选 HaWoR 高置信伪手标签
→ 对有手标签的片段：MANO 动作→GRQ token，做 Masked Chunk Prediction
→ LAP 逆动力学模块：片段边界两帧→潜动作向量
→ VLA 当前上下文隐藏态 ↔ LAP 潜动作向量做 joint alignment
→ 无手标签片段只做 latent alignment，不做动作 token 损失
→ 机器人后训练：预测隐藏态条件化 DiT flow-matching 动作头
```

- **LAP（Latent Action Perceiver）** 使用起止帧；**LSP（Latent State Perceiver）** 处理当前上下文；两者共享结构，用非对称 **EMA** 更新稳定对齐。视觉特征可用 **DINOv3 或 V-JEPA2**。
- 重要差别：JALA 刻意避开 LAPA 那种“必须重建完整未来帧才能得到好潜动作”的约束；有手标签时又用显式动作限制潜空间。
- 不是“完全无需动作标签”的方法；其强处恰恰是把有标签与无标签数据放在同一训练框架。

### C 路线对照

|                            | LAPA                    | LAWM                             | JALA                                               |
| -------------------------- | ----------------------- | -------------------------------- | -------------------------------------------------- |
| 潜动作由什么约束           | VQ 编码/解码未来帧      | 策略猜潜动作、世界模型重建未来帧 | 起止帧 IDM 潜动作与 VLA 隐藏态对齐；可选 MANO 监督 |
| 预训练必须有人手 3D 标签？ | 否                      | 否                               | 否，但混入有标签数据是关键设计                     |
| 是否预测未来像素/帧        | 是（量化模型训练）      | 是（RSSM 训练）                  | 不需要完整像素重建                                 |
| 最终控制适配               | 真实动作头微调          | 丢世界模型，策略微调             | DiT flow-matching 头后训练                         |
| 首要风险                   | 相机/背景变化混入潜动作 | 世界模型重建误差影响动作         | 两种隐空间对齐不稳、筛选质量影响学习               |

## 6. 路线 D/E：视频生成、未来建模与数据质量分工

### D1. DreamGen：用视频生成模型扩大“机器人自己做事”的训练数据

```text
少量特定机器人遥操作视频
→ 用 LoRA 微调视频世界模型（多数实验用 Wan2.1）学习该机器人外观/运动
→ 初始图 + 语言生成新任务/新环境的机器人视频
→ 给生成视频补伪动作：
   方案一：两帧→SigLIP-2 + DiT flow-matching IDM → 实际动作空间的伪动作
   方案二：LAPA → 无单位潜动作表示
→ 视频 + 指令 + 伪动作组成 neural trajectories
→ 用 Diffusion Policy / π0 / GR00T N1 训练或与真实轨迹联合训练
```

- IDM 方案需要**目标机器人的有动作数据**训练 IDM，虽然后续生成视频本身无标签；LAPA 方案可以不依赖目标机器人的真实动作，但之后控制适配更复杂。
- 它属于**离线合成训练集**，不是部署时每一步都生成视频；与 Dream2Flow 的使用位置不同。
- 核心失效链：视频生成物理不可信 → IDM/潜动作标错 → 策略学错。必须分别检查**指令符合、物体物理、机器人形态、动作回放**，不能只看画面“好看”。

### E1. LDA-1B：根据数据“有什么标签、质量怎样”分派任务

```text
EI-30k：真实/仿真机器人 + 有动作人手 + 无动作人类视频
→ 统一 LeRobot 格式、EEF/人手动作坐标、指令和质量标签
→ 未来图像用冻结 DINO 编成视觉 latent，不直接预测像素
→ MM-DiT 以 VLM 特征/任务 embedding 为条件
→ 四种任务可选择：
   policy：当前→动作
   forward dynamics：当前+动作→未来视觉 latent
   inverse dynamics：当前+未来→动作
   visual forecasting：当前→未来视觉 latent
→ 高质量有动作数据参与 policy/动力学；低质量动作主要做动力学；无动作视频做视觉预测
→ 用目标机器人少量数据后训练
```

- 最大启发不是“模型有多大”，而是**同一份混合质量数据不一定要用同一个损失**：差演示未必该拿来直接模仿，但物体运动过程仍可教动力学。
- 视觉约 3 Hz、动作约 10 Hz，模型处理异步视觉/动作流。相机外参被保留以区分头部运动和手运动。
- 需分清：**DINO latent** 是未来图像的视觉特征；LAPA/JALA 的 latent action 是对动作/变化的抽象，不是同一变量。

### E2. Being-H0.7：用未来图像教当前模型，但不在推理时“拍未来”

这篇已在 [B1](#b1-保留人手vitra-being-h-系列) 给出完整流程。它和 LDA-1B 的差异是：LDA-1B 直接把未来视觉 latent 当可生成目标，且提供多种条件任务；H0.7 让未来图像通过训练期 posterior **监督 latent query 隐藏态**，推理时只保留当前观测 prior。二者都不是像素视频生成路线。

### D/E 路线位置辨析

| 论文       | 视频/未来信息在何时起作用                                  | 最终给机器人的是什么     |
| ---------- | ---------------------------------------------------------- | ------------------------ |
| Dream2Flow | **推理时**生成未来视频，提物体 flow                        | 规划/RL 生成的动作       |
| DreamGen   | **训练数据制作时**生成大量机器人视频                       | 在伪标注数据上训练的策略 |
| LAWM       | **预训练时**重建未来帧；微调时丢世界模型                   | 微调后的策略动作         |
| LDA-1B     | **训练时**预测未来 DINO latent；模型也可按任务条件预测动作 | 直接预测动作块           |
| Being-H0.7 | **训练时**未来帧仅作后验隐藏态监督；推理无未来支路         | 直接预测动作块           |

## 7. 16 篇论文速查：不要把模型“输出”与“实验用的下游工具”混为一谈

| 论文            | 无标签视频最主要贡献            | 人→机器人接口              | 训练/执行终点                 | 一眼认出的技术名                   |
| --------------- | ------------------------------- | -------------------------- | ----------------------------- | ---------------------------------- |
| VidBot          | 3D 接触与轨迹                   | 形态无关可供性             | 轨迹扩散 + 测试时几何引导     | SfM、深度、TSDF、Perceiver         |
| Track2Act       | 未来 2D 点轨迹                  | 刚体变换→EEF + 残差        | 目标图像条件规划/策略         | CoTracker、DiT、RANSAC/PnP         |
| Dream2Flow      | 生成的物体 3D flow              | 物体目标→规划/RL           | 在线目标跟踪控制              | SAM2、CoTracker3、SpatialTrackerV2 |
| Masquerade      | 机器人化外观/2D waypoint        | Phantom 手→夹爪 + overlay  | 视觉预训练 + Diffusion Policy | HaMeR、SAM2、E2FGVI、FiLM          |
| Ego2Robot       | 多形态合成示范                  | 手→夹爪 + IK/渲染          | VLA 混合数据预训练            | WiLoR、DynHaMR、SAM3、ProPainter   |
| Qwen-RobotManip | H2R + 人手 + 异构真实机器人数据 | 统一 80 维动作与相机系 EEF | 多形态 VLA/VLM 双流           | CaPE、DiT、flow matching           |
| Do as I Do      | 4D 手—物交互                    | 仿真动力学灵巧手重定向     | 机器人可播放轨迹/数据         | HaWoR、SAM 3D、MoGe、MPPI          |
| VITRA           | 连续 MANO + 原子指令            | 人手预训练→机器人微调      | 灵巧手 VLA                    | HaWoR、MegaSAM、GPT-4.1            |
| Being-H0        | 离散 MANO 手动作                | action queries/MLP 后训练  | 灵巧手 VLA                    | HaMeR、GRQ、InternVL3              |
| Being-H0.5      | 人/多机器人统一槽位             | Unified Action Space       | 跨形态 VLA                    | HaWoR、MoT、MoF、MPG/UAC           |
| Being-H0.7      | 当前动作相关未来 latent         | 延续统一动作空间           | 未来监督的直接动作 VLA        | V-JEPA2.1、latent queries、双分支  |
| LAPA            | 前后帧潜动作                    | 有标签机器人微调           | latent VLA→机器人动作         | VQ-VAE/NSVQ、IDM/FDM               |
| LAWM            | 世界模型约束的潜动作            | 有标签机器人微调           | 策略预训练→动作               | RSSM/DreamerV3、BAKU/DP            |
| JALA            | IDM 潜动作与显式手标签融合      | DiT flow 头后训练          | 混合数据 VLA                  | LAP/LSP、EMA、GRQ、WiLoR           |
| DreamGen        | 合成机器人视频与伪动作          | IDM 或 LAPA 标动作         | 神经轨迹→策略训练             | Wan2.1、LoRA、SigLIP-2 IDM         |
| LDA-1B          | 混合质量数据里的未来动力学      | 统一手中心动作空间         | 多任务动作/未来 latent 模型   | LeRobot、DINO、MM-DiT              |

## 8. 以“想解决哪一步”为索引：技术选型与调用时的检查点

这不是对所有论文做公平 benchmark；“利弊”指从论文目标与方法设计可推导的**工程取舍**，具体效果要在同一数据条件下自己验证。

| 你需要什么       | 候选技术与对应论文                                    | 最小调用输入→输出               | 选用理由                              | 使用前先检查的局限                                             |
| ---------------- | ----------------------------------------------------- | ------------------------------- | ------------------------------------- | -------------------------------------------------------------- |
| 人/手/物体掩码   | SAM/SAM2/SAM3、Detectron2、Grounding DINO + SAM       | 帧/视频 + 框或文本提示 → mask   | 后续去人、限定点跟踪、隔离动态区域    | 遮挡、手出画、跨帧 mask 抖动；SAM 3 与 SAM 3D 不同             |
| 去手/去人        | E2FGVI、ProPainter                                    | 原视频 + 连续 mask → 修复视频   | 形成干净背景，便于叠机器人或看物体    | 遮挡背后内容是推断的，可能产生伪影；mask 漏一点就留残影        |
| 2D 点跟踪        | CoTracker/CoTracker3、SpatialTrackerV2 的追踪模块     | 视频 + 初始点 → 每帧位置/可见性 | 比完整手骨架更通用，适合物体运动      | 快速旋转、透明/无纹理物体、遮挡和相机运动                      |
| 3D 人手          | HaMeR、HaWoR、WiLoR+DynHaMR                           | RGB 图/视频 → MANO/21 点/腕姿   | 需要手指与腕部物理动作时              | 单目绝对尺度、遮挡、左右手错配、时间抖动                       |
| 3D 物体          | SAM 3D、物体 6D tracker                               | 图像+物体 mask/视频 → mesh/姿态 | 灵巧手接触与物理重定向需要物体几何    | 类别开放但刚体形状、遮挡和尺寸仍难；每帧独立 3D 不保证时序一致 |
| 相机轨迹与尺度   | SfM/COLMAP、EpicFields、MegaSAM、DroidCalib、MoGe-2   | 多帧 RGB → 内参/外参/深度/尺度  | 把移动相机下的手轨迹变为稳定坐标      | 动态场景、弱纹理、镜头畸变、单目尺度不确定                     |
| 单目深度         | Depth Anything、Metric3D、MoGe                        | RGB → 深度图                    | 3D 抬升、遮挡合成、尺度先验           | 相对/米制深度不要混用；不同帧尺度可能漂移                      |
| 人手→夹爪        | Phantom、Ego2Robot 的拇指+虚拟指尖                    | 21 点/MANO → TCP、姿态、开口    | 便于平行夹爪及大规模 H2R 数据         | 丢失手指接触、可达性；还需要 IK、碰撞检查                      |
| 人手→灵巧手      | VITRA 的关节映射、Being 后训练、Do as I Do 物理重定向 | MANO(+物体) → 机器人动作        | 保留更多灵巧信息                      | 形态差、接触力不可见、物理仿真成本高                           |
| 视频→潜动作      | LAPA、LAWM、JALA                                      | 相邻/边界帧 → 向量或离散 token  | 大规模无动作标签预训练                | 潜动作非物理动作；可能混进背景/相机变化                        |
| 视频生成         | Wan2.1 等（DreamGen/Dream2Flow）                      | 初始帧+指令 → 后续 RGB 帧       | 可扩任务/场景或提出目标               | 幻觉与物理一致性是核心瓶颈；生成帧不自带动作                   |
| 伪动作回填       | IDM、LAPA                                             | 两帧/视频 → 动作块或潜动作      | DreamGen 从无动作生成视频获得训练监督 | IDM 需同形态有标签数据；潜动作还需控制适配                     |
| 视觉未来预测     | RSSM、DINO latent、V-JEPA future embedding            | 当前(+动作) → 未来帧/特征       | 从无动作或低质量数据学习变化          | 像素损失偏外观；latent 损失未必保留精确接触                    |
| 多机器人动作统一 | 80 维槽位、Unified Action Space、EEF 坐标统一         | 各平台状态动作→共同字段+mask    | 混合训练不必每种机器人完全独立        | 单位、旋转、TCP、相机/基座定义必须审计                         |

### 调用任何 CV 组件的统一小实验模板

先取**同一段 10–30 秒视频**，保存输入视频、抽帧、提示词/框、模型版本、输出掩码/深度/关键点、每帧置信度、可视化叠图，以及**三类失败帧**。不要只看一张漂亮截图。再问：失败是感知误差、相机/深度误差、坐标转换误差，还是模型训练误差？这样学“怎么用”和“利弊”会比孤立背模型更快。

## 9. 建议的学习顺序：按依赖关系，不按论文年代

你目前的“先抓整体，再补技术细节”是合理的第一遍。现在的关键不是立刻逐式推导，而是尽快让每个新名词挂到**输入→输出→验证**上。建议沿下面顺序学；这些是阶段，不是每天的日程安排。

### 阶段 1：机器人数据契约和最小模仿学习

- 先完成你已经规划的 **LeRobot 小项目**：录制/加载一条示范，认清一条 episode 的图像、时间戳、关节/EEF 状态、动作、指令、相机视角，以及训练/验证/部署的区别。
- 学会分辨 **关节空间 vs EEF 空间、绝对目标 vs 相对增量、开环 vs 闭环、观察频率 vs 控制频率**。
- 最小产物：能打印并可视化一条 episode，解释“第 t 帧对应哪一个动作标签、下发到什么控制器”。
- 暂时不需要训练 7B VLA；先用 LeRobot 自带的小策略/模仿学习例子走通数据→训练→仿真或安全实机回放。实机必须遵循实验室安全和导师/管理员要求。

### 阶段 2：视频 CV 最基础的四种输出

依次接触：**分割 mask → 点跟踪 → 手关键点/MANO → 深度与相机**。先学会调用和看失败，再研究方法史。

1. mask：用 SAM 系列分割手/物/人，并在视频上检查跨帧一致性。
2. tracks：选物体上的几个点，调用 CoTracker，观察遮挡、出画、相机移动的失败。
3. hand：调用 HaMeR 或 HaWoR，分清单帧位置、MANO 关节、手腕轨迹和绝对尺度。
4. geometry：用现成深度与相机参数把一个 2D 点抬到 3D；再试把下一帧的点变换回第一帧。这里需要学**最小必要**的相机针孔模型与坐标变换，不必先钻研 SLAM 原理。

对应先读论文：**Track2Act → VidBot → VITRA**。前三篇让你从“像素点运动”逐步走向“度量 3D 轨迹与相机处理”，且不用一开始理解大 VLA 训练。

### 阶段 3：明确“视频里的动作”和“机器人动作”之间的鸿沟

- 先读 **Masquerade → Ego2Robot → Do as I Do**。
- 按一个例子对比：同一段捡起杯子的视频，三者输出分别是**视觉预训练监督**、**平行夹爪合成示范**、**多指灵巧手物理可执行轨迹**。成本、保留的信息和失败点完全不同。
- 关注工具使用级问题：mask 漏分后 ProPainter 会怎样？手关键点抖动导致 IK 怎样失败？2D overlay 与深度遮挡有什么区别？物体没有 3D mesh 时灵巧重定向为什么困难？
- 最小产物：一张自己画的“人手视频→机器人动作”误差传递图，不需要真的复现 15 形态。

### 阶段 4：学 VLA 的最小闭环，再读人手预训练论文

- 用一个小型、可跑通的 VLA/动作策略项目确认：**图像+指令+状态 → 未来动作块**，什么是预训练、监督微调、loss、action head、rollout；在仿真先测通，再考虑实机。
- 然后读 **Being-H0 → VITRA（回读模型部分）→ Being-H0.5 → Qwen-RobotManip**。
- 这时再比较：连续 DiT、离散 GRQ token、统一动作槽位、flow matching、VLM 数据联合训练各解决什么。你不必一开始推导 diffusion/flow matching，但至少要知道模型预测的是**噪声、速度场、离散 token 还是直接动作**。
- 最小产物：能对一个模型写出 `输入字段 / 动作字段 / 损失 / 数据来源 / 推理输出` 五行摘要。

### 阶段 5：如果视频没有可靠 3D 标签，学潜动作路线

- 读 **LAPA → LAWM → JALA**。LAPA 是清晰基线；LAWM 让你看到世界模型如何给潜动作自监督；JALA 解释如何把野外无标签视频与高质量手标签结合。
- 最小实验可只做“同一视频前后帧→潜动作/特征可视化”，**先别把潜动作当机器人控制指令**。
- 最小产物：解释为什么背景变化和相机运动会污染潜动作，JALA 又为什么不用像素重建作唯一目标。

### 阶段 6：视频生成和未来模型，最后再拓宽

- 读 **Dream2Flow → DreamGen → LDA-1B → Being-H0.7**。
- Dream2Flow 用生成视频当**推理目标**；DreamGen 用它造**训练数据**；LDA-1B 将无动作/低质量数据分派给未来建模；H0.7 把未来信息压进可部署的 latent reasoning。
- 最小产物：同一段生成视频先判断“物体运动是否可信”，再考虑是否值得做 3D flow 或伪动作标注。**不要因为视频视觉上真实就默认物理与动作标签正确**。

### 跨阶段持续做：一篇论文三遍读法

1. **第一遍：地图**。只回答：数据、监督、输出、桥接人机的接口、最容易失败的步骤。现在你已基本完成这一遍，不需要因为暂时看不懂公式而否定它。
2. **第二遍：一个关键模块**。沿 `输入文件/张量 → 调用的现成模型 → 输出字段/坐标/单位 → 可视化 → 错误过滤` 追到能在小样例上调用。每篇只选**最关键的一步**，不要一口气学完整栈。
3. **第三遍：研究判断**。比较同一环节的两种方法和论文消融：它究竟解决什么失败？多付出什么数据、算力、标定或实机代价？只有进入你自己的课题时才需要追公式和具体实现。

**不需要紧急改正“先看大致思路”**；需要改的是长期停留在“模型名很多，但不知道输入输出”。从现在起每读一篇都写一张 10 行以内的技术卡（输入、输出、坐标、依赖数据、关键模型、训练损失、测试需求、两个失败例子），再跑一个最小组件实例。这样会自然从粗读过渡到有目的的细读。

## 10. 接下来最值得补的空白，而不是再多背模型名

1. **相机与坐标几何是必修的薄层**：像素、深度、内外参、SE(3)、重投影、相机/世界/基座/EEF。无需先学完整多视几何证明，但不理解坐标与单位会使任何 H2R 数据无法审计。
2. **数据质量与时间同步**：一帧错一个动作、左右手颠倒、深度漂移、伪语言错物体、IK 不可达，都会让训练目标失真。建议把失败样本“留档并分类”当作研究技能。
3. **训练与数据泄漏**：同一个视频切成许多片段后，训练/验证可能共享场景和物体；生成视频也可能与目标环境太接近。即使暂不研究测评，至少要知道数据划分和泛化主张的边界。
4. **中间表示与控制接口的区别**：2D tracks、MANO、潜动作、3D flow 不是电机命令；总要问“最后谁将它转换成 EEF/关节命令？需要何种数据或控制器？”
5. **可执行性与安全性**：视觉上合理的轨迹不等于避碰、可达、抓稳。IK、碰撞检查、仿真回放、急停及人工监督在实机前必不可少；这部分可和你的 LeRobot 项目并行认识，但无需此刻精通。
6. **选研究问题时以瓶颈而非大模型为中心**：你未来可能做的是更好的手物伪标签、更稳的深度/坐标、更少伪影的合成、更可信的潜动作、数据清洗或质量路由，不一定要从头训练新 VLA。先找到一条路线里可测的错误传递，再决定论文题目。

## 11. 本地论文索引与核对位置

下面列出主要使用的**方法部分**，方便你读第二遍时回原文找技术名。文件内部的原有翻译可作辅助，但技术判断以对应 PDF 为准。

| 路线 | 论文                                                          | 建议回看位置                                  |
| ---- | ------------------------------------------------------------- | --------------------------------------------- |
| A    | [VidBot](papers/VidBot/VidBot.pdf)                            | §3.2 三维可供性提取；§3.3 粗到细模型          |
| A    | [Track2Act](papers/Track2Act/Track2Act.pdf)                   | §3.2 点轨迹预测；§3.3 刚体变换；§3.4 残差策略 |
| A/D  | [Dream2Flow](papers/Dream2Flow/Dream2Flow.pdf)                | §III-B 3D flow 提取；§III-C 动作推断          |
| B    | [Masquerade](papers/Masquerade/Masquerade.pdf)                | §III-B 编辑视频；§III-C 预训练/联合策略       |
| B    | [Ego2Robot](papers/Ego2Robot/Ego2Robot.pdf)                   | §3 动作/视觉对齐；附录 A 组件和清洗           |
| B    | [Qwen-RobotManip](papers/Qwen-RobotManip/Qwen-RobotManip.pdf) | §2 数据/H2R 清洗；§3 三层对齐；§4 训练        |
| B    | [Do as I Do](<papers/Do as  I Do/Do as I do.pdf>)             | §3.1 手物重建；§3.2 灵巧重定向                |
| B    | [VITRA](papers/Vitra/vitra_paper.pdf)                         | §3 三维手/相机、片段/语言；§4 VLA             |
| B    | [Being-H0](papers/BeingH0/BeingH0.pdf)                        | §4 物理指令微调；§5 UniHand 数据              |
| B    | [Being-H0.5](papers/BeingH0.5/BeingH0.5.pdf)                  | §3 UniHand-2.0；§5 统一空间/MoF/后训练        |
| B/E  | [Being-H0.7](papers/BeingH0.7/BeingH0.7.pdf)                  | §3 双分支 latent world-action 方法            |
| C    | [LAPA](papers/LAPA/LAPA.pdf)                                  | §3.1 量化；§3.2 潜动作预训练；§3.3 动作微调   |
| C    | [LAWM](papers/LAWM/LAWM.pdf)                                  | §3.1 世界模型潜动作预训练；§3.2 动作微调      |
| C    | [JALA](papers/JALA/JALA.pdf)                                  | §4 joint alignment/LAP/LSP；§5 数据筛选       |
| D    | [DreamGen](papers/DreamGen/DreamGen.pdf)                      | §2.1–2.4 四阶段生成/伪标签/策略训练           |
| E    | [LDA-1B](papers/LDA-1B/LDA-1B.pdf)                            | §III 四任务及 MM-DiT；§IV EI-30k 数据         |
